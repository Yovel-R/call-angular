# DealVoice UI Contract

Admin feature modules use `UI_COMPONENT_GUIDE.md` at the workspace root as the visual contract.

Keep feature screens in a single workspace frame with a left rail, fixed toolbar, independently scrollable panes, restrained table typography, and the same filter/search/button treatments as the employee dashboard.
